import { createId } from 'src/test-utils/helpers/createId';

export const createParentDto = {
  firstName: 'Eliza',
  lastName: 'Spark',
  birthday: new Date('1985-03-18'),
};

export const userId = createId();
